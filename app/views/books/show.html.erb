<h3><%= @book.title %></h3>
<h4><%= @book.author %></h4>
<p><%= @book.description %></p>

<%= link_to 'Back', root_path %>
<%= link_to 'Edit', edit_book_path %>
<%= link_to 'Delete', book_path(@book), method: :delete, data: { confrim: "Are you sure?"} %>

